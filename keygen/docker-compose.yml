services:
  keygen:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ${KEYGEN_CONTAINER_NAME:-keygen-source}
    ports:
      - "${KEYGEN_PORT:-19090}:9090"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
